{% extends 'base.html.twig' %}

{% block body %}
    {% form_theme form 'bootstrap_5_layout.html.twig' %}
    {{ form_start(form) }}
        {{ form_row(form.title, {'label': 'Titre :'}) }}
        {{ form_row(form.description, {'label': 'Description (facultatif)' }) }}
        {{ form_row(form.submit, {'label' : label})}}
    {{ form_end(form) }}
{% endblock %}